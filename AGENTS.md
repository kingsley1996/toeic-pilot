# ToeicPilot — lưu ý cho AI agent

- Task tạo/sửa cảnh 3D (`apps/web/src/content/scenes/**`,
  `apps/web/src/components/scene-*.tsx`, `planning/scenes/**`): đọc
  `planning/docs/SPEC-VISUAL-VOCAB-3D.md` §7–§9 TRƯỚC khi đụng code,
  verify theo §7.11 (tsc + eslint + e2e visual-vocab + regen preview + mắt xem ảnh).
- Luật đầy đủ theo vùng mã: xem `CLAUDE.md` (bảng `.claude/rules/`).
