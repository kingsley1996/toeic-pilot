"""Các suite eval — mỗi suite đổi vì lý do của miền nó đo.

- `coach`: validators + prompt của trợ giảng.
- `shape`: định dạng dòng giải thích backfill.
- `retrieval`: knowledge base + đường tìm kiếm.
- `planner`: lớp chọn của planner.
"""
